# Normalise to LF in the repository so the format gate agrees across the
# windows/ubuntu/macos CI matrix.
* text=auto eol=lf

*.png binary
*.jpg binary
*.webp binary
*.ico binary
*.pdf binary
*.db binary
